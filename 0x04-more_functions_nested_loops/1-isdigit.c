#include "main.h"
/**
 * _isdigit(int c) - checks for digit
 * @c: integer being checked
 * Return: 1 if c is a digit and 0 if otherwise
*/
int _isdigit(int c)
{
  if (c >= 48; && >= 57)
  {
    return(1);
  }
  else
  {
    return(0);
  }
  